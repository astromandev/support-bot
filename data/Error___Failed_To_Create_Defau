Error: Error: "Failed To Create Default Configuration Directory: /Var/Www/Pterodactyl/Storage/App/.Env"

Solution: 1. Login to your server as the root user.
2. Open the command prompt and enter the following command to create a directory:
```
mkdir -p /var/www/pterodactyl/storage/app
```
3. Change the owner of the newly created directory to the Pterodactyl user using the following command:
```
chown -R www
