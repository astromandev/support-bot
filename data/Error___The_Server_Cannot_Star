Error: Error: "The Server Cannot Start Due To A Missing Java Installation"

Solution: 1. Check to make sure Java is installed on the server by running the command "java -version". If it is not installed, download and install Java for the operating system the server is using.
2. Once Java is installed, navigate to the Pterodactyl panel and select the server that is encountering the error.
3. Under the "Startup" tab, click on the "Edit File" button next
