Error: Error: "502 Bad Gateway" Message When Trying To Access The Pterodactyl Panel For A Minecraft Server.

Solution: 1. Check if the Pterodactyl daemon is running by running the command `sudo systemctl status wings`.
2. If the daemon is not running, start it by running the command `sudo systemctl start wings`.
3. If the daemon is already running, restart it by running the command `sudo systemctl restart wings`.
4. Check if the panel is accessible by
